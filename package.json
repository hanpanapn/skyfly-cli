{
  "name": "skyfly",
  "version": "1.0.8",
  "description": "Using webpack to quickly initialize a project",
  "repository": "dff",
  "bin": {
    "skyfly": "./bin/main.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf lib && babel src --out-dir lib"
  },
  "author": "jaden_hsq",
  "license": "ISC",
  "dependencies": {
    "commander": "^4.0.1",
    "path": "^0.12.7",
    "through2": "^3.0.1",
    "vinyl-fs": "^3.0.3"
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ],
    "plugins": [
      "add-module-exports",
      "transform-runtime"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-add-module-exports": "^1.0.2",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "babel-runtime": "^6.26.0"
  }
}
